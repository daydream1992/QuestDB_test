# `2_compute/` — 已废弃空目录

本目录保留为空作为历史标记。原计划作为"二级计算"流水线入口，但实际未填入任何文件。

## 为什么不删除

按 2026-07-05 整理约定："不删除"。本目录与 `2_kline/` 同名（一个含实际内容 `qd_02_synth_kline.py`，
一个为空），保留此空目录作为"曾经存在过的命名冲突"的可视化提示。

## 取回方式

```bash
rm 2_compute/DEPRECATED.md
```