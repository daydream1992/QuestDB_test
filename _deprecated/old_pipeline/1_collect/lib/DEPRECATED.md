# `1_collect/lib/` — 已废弃空目录

本目录保留为空作为历史标记。原计划作为 `1_collect/` 下的工具库，但实际未填入任何文件。

## 为什么不删除

按 2026-07-05 整理约定："不删除"。本目录保留作为"曾经打算有但实际没有"的视觉提示，
提醒维护者这里**不要**再新建工具模块——`1_collect/` 是旧版流水线入口，新版请走 `collect/`。

## 取回方式

如果未来想"复活"此目录：

```bash
# 删除本 README 让目录恢复成空
rm 1_collect/lib/DEPRECATED.md
```

无需从历史取回（目录从未含文件）。