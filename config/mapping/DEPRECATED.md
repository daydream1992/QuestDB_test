# `config/mapping/` — 已废弃空目录

本目录保留为空作为历史标记。原计划作为"板块-个股映射"的 config 子目录，但实际映射数据
落在 `指数板块个股映射/` 和 `lib/relation_graph.py` 处理。

## 为什么不删除

按 2026-07-05 整理约定："不删除"。保留此空目录作为"曾规划的 config 子目录"的可视化提示。

## 取回方式

```bash
rm config/mapping/DEPRECATED.md
```